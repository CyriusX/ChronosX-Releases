# Vitest runs the UI in a normal browser-like environment (no desktop bridge).
# For many component tests we still want IPC-dependent components to behave as if
# they are running inside AgentHost, so we opt into the Mock IPC client in tests.
VITE_USE_MOCK_IPC=true

